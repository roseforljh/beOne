/**
 * API 配置文件
 * 
 * 注意: 此文件会在运行 npm start 时自动生成
 * 手动修改的内容会被覆盖
 * 
 * 如需自定义配置,请修改项目根目录的 deploy-simple.js
 */

// 默认配置(开发环境)
export const API_CONFIG = {
  API_URL: 'http://localhost:5000'
};

export const IS_DEBUG_MODE = true;
export const AUTO_DETECTED_IP = 'localhost';